# Before launch this code you must install selenium
(If no warings you just run code (: )
```
pip install selenium
```

This code believes that you have identified possible sites that you want to get to, if you use gobuster)
And also you can change variables with url


And you must download chromeDriver, you can download here https://developer.chrome.com/docs/chromedriver/downloads
